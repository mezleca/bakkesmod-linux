#!/usr/bin/env bash
set -euo pipefail

python -m venv .venv
source .venv/bin/activate
pip install -e .

exec env \
  BAKKESLINUX_CUSTOM_INJECTOR="${BAKKESLINUX_CUSTOM_INJECTOR:-$PWD/build/simple_injector.exe}" \
  bakkesmod "$@"
